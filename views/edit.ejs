<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/stylesheet/user.css">
</head>
<body>
    <div class="container">
        <div class="user-card">
            <form action="/users/<%= user._id %>/edit" method="POST" style="width:100%;display:flex;flex-direction:column;align-items:center;">
                <img src="<%= user.url %>" alt="User Image" id="preview-img">
                <input type="text" name="name" value="<%= user.name %>" placeholder="Name" required style="margin:12px 0;width:90%;padding:10px;border-radius:8px;border:1px solid #ccc;">
                <input type="email" name="email" value="<%= user.email %>" placeholder="Email" required style="margin:12px 0;width:90%;padding:10px;border-radius:8px;border:1px solid #ccc;">
                <input type="url" name="url" value="<%= user.url %>" placeholder="Image URL" required style="margin:12px 0;width:90%;padding:10px;border-radius:8px;border:1px solid #ccc;" oninput="document.getElementById('preview-img').src=this.value">
                <button type="submit" style="margin-top:18px;padding:10px 32px;border-radius:8px;background:#007bff;color:#fff;border:none;font-size:1em;cursor:pointer;transition:background 0.2s;">Save Changes</button>
            </form>
        </div>
    </div>
</body>
</html>